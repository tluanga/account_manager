import 'package:flutter/foundation.dart';

class TrialBalance extends ChangeNotifier {
  // --Get all ledgers
  // --Calculate the balance of the ledger by substrating from each side which eever is bigger
}
